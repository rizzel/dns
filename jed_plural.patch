--- htdocs/js/jed.js	(revision )
+++ htdocs/js/jed.js	(revision )
@@ -266,7 +266,7 @@
       var val_idx;
       if (val === undefined) {
         // No value passed in; assume singular key lookup.
-        val_idx = 0;
+        val_idx = 1;
 
       } else {
         // Value has been passed in; use plural-forms calculations.
